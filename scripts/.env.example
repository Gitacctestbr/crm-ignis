# ─── API Keys ──────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-api03-SUBSTITUA_PELA_SUA_CHAVE

# ─── Google Sheets ─────────────────────────────────────────────────────────────
# ID da planilha — encontrado na URL:
#   sheets.google.com/spreadsheets/d/  <<ID_AQUI>>  /edit
GOOGLE_SHEETS_ID=17LLhAREU7dZFag4JPGCFt_1H0gvuCyTbE9Rc_2F9O7s

# Nome da aba/tab da planilha onde os dados serão inseridos (padrão: Prints)
GOOGLE_SHEET_TAB=Prints

# ─── Pastas e credenciais (gerenciados pelo Docker via volumes) ─────────────────
# NÃO altere estas variáveis — elas são sobrescritas pelo docker-compose.yml.
# Edite os volumes no docker-compose.yml se precisar mudar os caminhos do host.
WATCH_FOLDER=/watch
PROCESSED_FOLDER=/processed
GOOGLE_CREDENTIALS_PATH=/app/google.json
